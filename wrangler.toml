# Cloudflare Pages + Functions (Full-stack on Cloudflare)

name = "pass"
compatibility_date = "2024-01-15"
pages_build_output_dir = "dist"

# D1 Database for data storage
[[d1_databases]]
binding = "DB"
database_name = "pssm"
database_id = "54dc6d7d-1f71-46a9-957f-40fc1e590832"

# Environment variables
[vars]

# IMPORTANT: Set these as secrets in Cloudflare Pages settings
# - JWT_SECRET (for token signing)
# - MASTER_PASSWORD (for authentication)
# DO NOT commit secrets to version control